# Local dev only: point the frontend at a separately-running backend.
# In production the static export is served by FastAPI at the same origin,
# so this is left UNSET (API calls use relative /api paths).
# NEXT_PUBLIC_API_BASE=http://localhost:8000
